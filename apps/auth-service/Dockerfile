# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @or-smart-analytics/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t or-smart-analytics-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system or-smart-analytics-auth-service && \
          adduser --system -G or-smart-analytics-auth-service or-smart-analytics-auth-service

COPY apps/auth-service/dist or-smart-analytics-auth-service/
COPY apps/auth-service/package.json or-smart-analytics-auth-service/
RUN chown -R or-smart-analytics-auth-service:or-smart-analytics-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix or-smart-analytics-auth-service --omit=dev -f install

CMD [ "node", "or-smart-analytics-auth-service" ]
